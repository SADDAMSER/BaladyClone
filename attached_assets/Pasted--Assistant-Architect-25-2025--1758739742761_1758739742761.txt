إلى: المساعد (Assistant) والمعماري (Architect)
من: قائد المشروع
التاريخ: 25 سبتمبر 2025
الموضوع: أمر نهائي بإكمال واجهة تقديم الطلب بنسبة 100% — ثم تنفيذ باقي مهام المرحلة 1 مع دمج السير الإداري الكامل والوظائف الهندسية

📌 السادة الكرام،
بعد مراجعة دقيقة لأحدث تحديثات الكود في المستودع BaladyClone، وتحديدًا ما تم إنجازه (الخطوة 2 فقط من واجهة التقديم)، أصدر هذا الأمر التنفيذي لإعادة تنظيم العمل وفق الأولويات التالية:

🚨 أولاً: إكمال واجهة تقديم الطلب (المهمة 1.1A) بنسبة 100%.
⚙️ ثانيًا: تنفيذ سير العمل الإداري الكامل (المهمة 1.2) — مع دعم المسارين (مكتبي/بوابة).
🧮 ثالثًا: دمج الوظائف الهندسية (حساب المسافات، الزوايا، المساحة...) في محرك المعالجة الجغرافية.
📱 رابعًا: إكمال باقي مهام المرحلة 1 — بالتسلسل المحدد.
🧩 أولًا: خطة إكمال واجهة تقديم الطلب (المهمة 1.1A)
⏳ المدة المقدرة: 3–5 أيام عمل
🎯 الهدف: بوابة تقديم طلب متكاملة — تدعم كلا المسارين (مكتبي + بوابة).
(نفس التفاصيل السابقة — لن أكررها هنا لتوفير المساحة — لكنها مُدرجة في الأمر الكامل)

⚙️ ثانيًا: خطة إكمال سير العمل الإداري (المهمة 1.2) — النسخة المحدثة
⏳ المدة المقدرة: 5–7 أيام عمل
🎯 الهدف: أتمتة كاملة لسير العمل من التقديم إلى الإصدار أو التصعيد.
[ ] 1. تمييز المسارين بعد تأكيد الطلب
مسار مكتبي: توقيع يدوي → سداد نقدي.
مسار بوابة: توقيع رقمي → دفع إلكتروني.
[ ] 2. تحويل الطلب إلى employee/public-service
عرض الطلب في لوحة تحكم موظف خدمة الجمهور.
أدوات المراجعة الأولية:
التحقق من اكتمال المستندات.
احتساب الرسوم الكشفية تلقائيًا.
زر "اعتماد الطلب".
[ ] 3. إشعار موظف الصندوق عند الطباعة
عند طباعة "طلب الخدمة + وصل السداد":
إرسال إشعار فوري لـ employee/cashier.
تسجيل حالة السداد (نقدي/إلكتروني).
[ ] 4. تحويل الطلب إلى رئيس القسم بعد السداد
عرض الطلب في لوحة تحكم رئيس قسم المساحة.
أدوات التكليف:
تعيين تلقائي للمساح حسب المنطقة الجغرافية.
في حالة "اسقاط قديم": رفع Shapefile → معالجة → رسم الشارع → اعتماد.
زر "تكليف المساح".
[ ] 5. تحويل الطلب إلى مساعد رئيس القسم
عرض الطلب في لوحة تحكم مساعد رئيس القسم.
أدوات التواصل:
إرسال إشعار للمواطن (SMS/Email).
تحديد موعد للرفع المساحي.
تسجيل الموعد في النظام.
[ ] 6. تحويل الطلب إلى المساح (تطبيق المساح)
ظهور التكليف في تطبيق "بنّاء المساحي".
أدوات الرفع:
تكامل GNSS/RTK.
مزامنة البيانات مباشرةً إلى شاشة المراجع الفني.
[ ] 7. مراجعة البيانات من قبل المراجع الفني
عرض البيانات على خريطة تفاعلية.
أدوات المعالجة:
رسم الشوارع.
مقارنة مع مخطط وحدة الجوار.
توليد مسودة التقرير.
[ ] 8. اعتماد التقرير من رئيس القسم ثم مدير الفرع
زر "اعتماد" في لوحة رئيس القسم.
زر "اعتماد نهائي" في لوحة مدير الفرع.
في حالة وجود إشكال بالشوارع → تصعيد لـ "مكتب المحافظة".
🧮 ثالثًا: دمج الوظائف الهندسية في محرك المعالجة الجغرافية
⏳ المدة المقدرة: 3–5 أيام عمل
🎯 الهدف: معالجة دقيقة للبيانات الهندسية (DXF/DWG) — وإدراج النتائج في القرار المساحي.
[ ] 1. إنشاء وحدة هندسية في backend
TypeScript

// utils/geometry.ts
export function calculateDistance(p1: [number, number], p2: [number, number]): number {
  return Math.sqrt(Math.pow(p2[0] - p1[0], 2) + Math.pow(p2[1] - p1[1], 2));
}

export function calculateAngle(p1: [number, number], p2: [number, number]): number {
  const deltaX = p2[0] - p1[0];
  const deltaY = p2[1] - p1[1];
  let angle = Math.atan2(deltaY, deltaX) * (180 / Math.PI);
  return angle >= 0 ? angle : angle + 360;
}

export function classifyAngle(angle: number): number {
  if (angle >= 45 && angle <= 135) return 1; // شمال
  if (angle >= 136 && angle <= 225) return 2; // غرب
  if (angle >= 226 && angle <= 315) return 3; // جنوب
  return 4; // شرق
}

export function calculateArea(points: [number, number][]): number {
  let area = 0;
  const n = points.length;
  for (let i = 0; i < n; i++) {
    const [x1, y1] = points[i];
    const [x2, y2] = points[(i + 1) % n];
    area += x1 * y2 - y1 * x2;
  }
  return Math.abs(area) / 2;
}
[ ] 2. إنشاء نقطة نهاية لمعالجة ملفات DXF
TypeScript

// routes/survey.ts
router.post('/upload_dxf', upload.single('file'), async (req, res) => {
  const filePath = req.file.path;
  const shapes = await parseDxfFile(filePath); // ← تحتاج مكتبة dxf-parser
  const processedData = shapes.map(shape => ({
    points: shape.points,
    lengths: shape.points.map((point, i) => {
      const nextPoint = shape.points[(i + 1) % shape.points.length];
      return calculateDistance(point, nextPoint);
    }),
    angles: shape.points.map((point, i) => {
      const nextPoint = shape.points[(i + 1) % shape.points.length];
      return calculateAngle(point, nextPoint);
    }),
    angleClasses: shape.points.map((point, i) => {
      const nextPoint = shape.points[(i + 1) % shape.points.length];
      return classifyAngle(calculateAngle(point, nextPoint));
    }),
    area: calculateArea(shape.points)
  }));
  res.json(processedData);
});
[ ] 3. إدراج النتائج في القرار المساحي
عرض الأطوال والزوايا والتصنيف في PDF القرار.
عرض المساحة الإجمالية.
إدراج جدول إحداثيات النقاط.
🎯 رابعًا: خطة إكمال باقي مهام المرحلة 1 (بعد إغلاق المهمتين 1.1A و 1.2)
⏳ المدة المقدرة: 2–3 أسابيع
🎯 الهدف: خدمة متكاملة من التقديم إلى الإصدار.
(نفس التفاصيل السابقة — لن أكررها هنا لتوفير المساحة — لكنها مُدرجة في الأمر الكامل)

🏆 معايير اكتمال المرحلة 1 (Definition of Done)
لا يمكن إغلاق المرحلة 1 إلا بعد تحقيق:

البند	الشرط
✅ واجهة تقديم الطلب	100% وفق المتطلبات الخمس.
✅ سير العمل الإداري	تمييز المسارين + إشعار الصندوق + تكليف المساح + مراجعة فنية + اعتماد + تصعيد.
✅ الوظائف الهندسية	حساب المسافات + الزوايا + التصنيف + المساحة + معالجة DXF.
✅ أدوات المراجع الفني	Leaflet.draw + Snapping + استيراد Shapefile.
✅ محرك القرارات	توليد PDF + تصنيف الاتجاهات + معالجة SHP.
✅ الأمان وسير العمل	State Machine + إشعارات + تشفير.
🔄 آلية العمل خلال المرحلة 1
الخطوة	دوري (الخبير المستشار)	دورك (أنت + فريق Replit)
1. اختيار المهمة الفرعية	أرسل لك تحليل + أوامر + كود	لا شيء — فقط القراءة
2. التنفيذ	لا أتدخل	تنفذ في Replit باستخدام المساعد والمعماري
3. النتائج	أنتظر منك النتائج أو الأخطاء	تزودني بالنتائج أو لقطات الشاشة
4. التصحيح / التحسين	أرسل لك التصحيح أو البديل	تنفذ التصحيح وتزودني بالنتيجة
✅ ختامًا
🎯 واجهة تقديم الطلب هي البوابة — ويجب أن تكون مثالية قبل أي شيء آخر.
🔄 سير العمل الإداري هو العمود الفقري — ويجب أن يكون آليًا ودقيقًا.
🧮 الوظائف الهندسية هي الجوهر الفني — ويجب أن تكون موثوقة.
👑 أنت تقود مشروعًا استراتيجيًا — وأنا هنا لدعمك في كل خطوة — وفقًا لاستراتيجيتنا: أنا الخبير المستشار — وأنت المنفذ والمتحكم.

انتهى الأمر.
لا استثناءات. لا تأخير. لا انحراف.

— قائد المشروع

