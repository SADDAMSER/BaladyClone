إلى: المساعد (Assistant) والمعماري (Architect)
من: قائد المشروع
التاريخ: 25 سبتمبر 2025
الموضوع: أمر نهائي بتبني آلية "الاختبار الشامل" لكل مهمة — وعدم الإبلاغ عن الإنجاز إلا بعد اجتياز جميع اختبارات القبول

📌 السادة الكرام،
بعد مراجعة دقيقة لسجلات التنفيذ السابقة، تبين أن:

❗ الإبلاغ عن "إنجاز جزئي" يؤدي إلى:

هدر الوقت في المراجعات المتكررة.
فقدان الثقة في نتائج التنفيذ.
تأخير المشروع بسبب إعادة العمل.
بناءً عليه — أصدر هذا الأمر التنفيذي لتبني آلية الاختبار الشامل لكل مهمة — وفق الضوابط التالية:

🧩 أولًا: تعريف "الاختبار الشامل"
هو مجموعة من اختبارات القبول (Acceptance Tests) التي يجب أن تجتازها المهمة — قبل الإبلاغ عن إنجازها.

مثال على اختبارات واجهة تقديم الطلب:

TypeScript

// tests/acceptance/surveying-decision-form.test.ts
describe('واجهة تقديم الطلب — الخطوة 1', () => {
  it('يجب أن يملأ الحقول تلقائيًا عند إدخال رقم هوية مسجل', async () => {
    // ...
  });
});

describe('واجهة تقديم الطلب — الخطوة 2', () => {
  it('يجب أن تعرض GeoTIFF إن وُجد عند اختيار وحدة جوار', async () => {
    // ...
  });
});

describe('واجهة تقديم الطلب — الخطوة 3', () => {
  it('يجب أن يفتح Modal عند اختيار "إصدار رخصة بناء قديم"', async () => {
    // ...
  });
});

// ... وهكذا لكل خطوة
✅ ثانيًا: شروط الإبلاغ عن الإنجاز
لا يُسمح بالإبلاغ عن إنجاز أي مهمة إلا بعد:

✅ إكمال 100% من المتطلبات الوظيفية.
✅ اجتياز 100% من اختبارات القبول.
✅ تقديم تقرير نتائج الاختبار (Pass/Fail).
📋 ثالثًا: قالب تقرير نتائج الاختبار
Markdown

### 📊 تقرير نتائج الاختبار: [اسم المهمة]

| البند | الحالة | التفاصيل |
|-------|--------|----------|
| ✅ المتطلبات الوظيفية | ✔️ مكتملة | قائمة بالمتطلبات المنفذة. |
| ✅ اختبارات القبول | ✔️ 25/25 نجحت | رابط لنتائج الاختبار. |
| ❌ الأخطاء | ➖ لا يوجد | أو قائمة بالأخطاء إن وُجدت. |

✅ **جاهز للانتقال للمهمة التالية.**
🔄 رابعًا: آلية العمل الجديدة
الخطوة	دوري (الخبير المستشار)	دورك (أنت + فريق Replit)
1. بدء المهمة	أرسل لك المتطلبات + اختبارات القبول	لا شيء — فقط القراءة
2. التنفيذ	لا أتدخل	تنفذ المهمة بنسبة 100%
3. الاختبار	لا أتدخل	تشغيل اختبارات القبول — وإرسال التقرير
4. التصحيح	أرسل لك التصحيح إذا فشل الاختبار	تنفذ التصحيح → تعيد الاختبار → ترسل التقرير
5. الإنجاز	أتحقق من التقرير → أُعلن الإنجاز	لا شيء — فقط الانتظار
🚀 خامسًا: التطبيق الفوري على المهمة 1.1A
سأجهز لك الآن اختبارات القبول لواجهة تقديم الطلب — وسأرسلها لك خلال ثوانٍ.

